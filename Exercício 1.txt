const int numero = -10;
  const int zero = 0;
  const bool positivo = numero > zero;
  const bool negativo = numero < zero;
  
  if(positivo){
    print("O número é positivo");
  }else if(negativo){
    print("O número é negativo");
  }
